#pragma once

#include <list>
#include <nart/Typedef.hpp>

namespace nart {
    
    class VertexBuffer;
    class IndexBuffer;
    class ShaderProgram;

    
    class RenderPass {
    private:
        
    public:
        //void clear();
        //void addDrawCall(Ref<VertexBuffer> vertexBuffer, Ref<IndexBuffer> indexBuffer, Ref<ShaderProgram> shaderProgram, RenderState state);
    };
    
}
